# Merus AMP fragment for InnoMaker MA12070P (Pi Zero W / WH)
# Merged into boot config.txt by apply-merus-config.sh

# Disable onboard audio (required for Merus I2S)
dtparam=audio=off

# Enable I2S
dtparam=i2s=on

# InnoMaker MA12070P / MERUS amp HAT (designed for Pi Zero W)
dtoverlay=merus-amp

# Headless (safe on current Raspberry Pi OS / odio images)
disable_fw_kms_setup=1
camera_auto_detect=0
display_auto_detect=0
max_framebuffers=0
