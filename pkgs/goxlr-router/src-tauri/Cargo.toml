[package]
name = "goxlr-router"
version = "0.1.0"
description = "PipeWire audio routing switchboard UI"
edition = "2021"

[lib]
name = "goxlr_router_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
